{
  "name": "dispatched-code",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Dispatched Code — Claude delegates to parallel OpenCode workers (MCP server)",
  "scripts": {
    "spike": "bun run spike/spike.ts",
    "mcp": "bun run src/mcp/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:e2e": "OC_E2E=1 bun test test/e2e"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  }
}
